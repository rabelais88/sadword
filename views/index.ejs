<!DOCTYPE html>
<html>
  <% include partials/header %>
  <!-- body -->
  <body>
    <%include partials/nav %>
    <div class="container-fluid content darken">
      <% data.forEach(function(el,i){ %>
        <p><%= el.article_content %></p>
        <small class="text-muted"><%= el.writer_ip %></small>
        <form action="/<%= i %>/">
          <button type="submit" class="btn btn-link" value="up" name="act"><i class="fa fa-thumbs-o-up" aria-hidden="true"></i>공감</button>
          <button type="submit" class="btn btn-link" value="down" name="act"><i class="fa fa-exclamation" aria-hidden="true"></i>신고</button>
          <button type="submit" class="btn btn-link" value="edit" name="act"><i class="fa fa-pencil-square-o" aria-hidden="true"></i>수정</button>
          <button type="submit" class="btn btn-link" value="delete" name="act"><i class="fa fa-trash" aria-hidden="true"></i>삭제</button>
        </form>
        <hr>
      <% }) %>
    </div>
  </body>
</html>